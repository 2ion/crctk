AM_CFLAGS = -Wall -Os -march=native $(libcdb_CFLAGS)
AM_LDFLAGS = -lz $(libcdb_LIBS)

bin_PROGRAMS = crctk librealpathtest
crctk_SOURCES = crctk.c realpath.c realpath.h
librealpathtest_SOURCES = realpath.c realpathtest.c realpath.h

