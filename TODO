- when stripping: remove punctuation only if it is symmetrical
- append to database instead of overwriting (constant database -> works
  only by reading everything back first and then writing everything out
  again.
